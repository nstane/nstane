import * as React from 'react';
import { isTSAnyKeyword } from '@babel/types';
import App from '../App';


describe("<App/>", () => {
    it('should update offerListOffsetTop', () => {
        const app = new App({});
        
        
      });
})